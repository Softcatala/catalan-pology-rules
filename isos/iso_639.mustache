#
# Language code rules
#
# License: GPLv3
#
# The objective of this rules is to bring consistency in the translation of languages
# names across translation projects.
# 
# These rules have been generated using ISO_639-3 from the http://translationproject.org/
# as a source.
#
 

# Remove XML entities.
include file="filter.include"

# Remove accelerator and  hyphen inside words.
addFilterRegex match="(\&|\-)" on="pmsgid,pmsgstr"


{{#rules}}
{^{{source}}$}i
id="language-{{source}}"
{{#targets}}
valid msgstr="{{target}}"
{{/targets}}
hint="El nom de llengua «{{source}}» es tradueix per «{{all_targets}}»."

{{/rules}}
